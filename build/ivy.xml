<ivy-module version="2.0">

  <info
    organisation="uk.ac.gate"
    module="gate-plugin-learningframework"
    revision="2.0-SNAPSHOT">
    <description homepage="https://github.com/GateNLP/gateplugin-LearningFramework/" />
  </info>

  <configurations>
    <conf name="default" />
  </configurations>

  <dependencies defaultconf="*->master(default),runtime(default)" >
    <!-- YAML is not found in the GUI during runtine of the LF when added this way, but added as a 
         static jar all is fine.
    <dependency org="org.yaml" name="snakeyaml" rev="1.16" />
    -->
    <dependency org="cc.mallet" name="mallet" rev="2.0.8" />
    <dependency org="org.jdom" name="jdom" rev="1.1"/>
    <dependency org="gate.lib" name="interaction" rev="1.0-SNAPSHOT"/>
  </dependencies>
</ivy-module>

